import brandLogo from '../img/brand.png'; 

const BrandComponent = () => {
    return (
        <img src={brandLogo} alt="logo" title="logo de la pizzeria" />
    );
};

export default BrandComponent;